# Binaries
bin/
# Stray root build output (the build target writes to bin/app; a bare
# `go build .` would otherwise drop an `app` binary here).
/app

# Database
*.db
*.db-shm
*.db-wal
/data/

# Directories
.notes/
.misc/
.tmp/

# Local env (keep .env.example tracked)
.env

# Superpowers scratch (SDD ledgers, brainstorm mockups)
.superpowers/

# Playwright MCP browser-driving artifacts (snapshots, screenshots)
.playwright-mcp/
